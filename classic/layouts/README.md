# Classic Layout Artifacts

Drop renderer-generated layout JSON and diff outputs here.

Common examples:
- `renderer_dom_layout.json` – layout extracted from the running renderer.
- `renderer_vs_reference_diff.json` – output of `compare-layouts`.

Reference truth stays in `reference/layouts/layout.json`; copy fresh renderer outputs here for side-by-side tracking.
